
# M3E Canonical

So there is two things to canonical layouts. One, the adaptive scaffold.
Two, the canonical layouts themselves.

So showing steps to build the adaptive scaffold to work with Go Router 
which happens to follow the same basice steps in the codelabs example.

